## 8.8 Changing pg_ref style defaults

Changing the defaults of the `pg_ref` is done just after `pgInit`. Changing the defaults is shown in section 3.3, [A Different Default Font, Style, Paragraph](#3.3-a-different-default-font,-style,-paragraph).

